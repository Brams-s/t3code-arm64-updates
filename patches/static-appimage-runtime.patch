diff --git a/scripts/build-desktop-artifact.ts b/scripts/build-desktop-artifact.ts
--- a/scripts/build-desktop-artifact.ts
+++ b/scripts/build-desktop-artifact.ts
@@ -2150,6 +2150,12 @@ const createElectronBuilderConfig = Effect.fn("createElectronBuilderConfig")(fun
         },
       },
     };
+
+    // TODO: Remove this once electron-builder defaults to the static AppImage runtime.
+    // Ubuntu 22.04+ does not install libfuse2 by default, so we pin the static runtime here.
+    buildConfig.toolsets = {
+      appimage: "1.0.2",
+    };
   }
 
   if (platform === "win") {
